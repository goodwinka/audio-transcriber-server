# ── Обязательные ────────────────────────────────────────────
flask==3.1.*
vosk==0.3.*

# ── Faster-Whisper (CPU int8, оптимально для Intel) ──────────
# Раскомментируйте или установите через install.bat:
# faster-whisper==1.*

# ── OpenVINO (Intel iGPU/CPU) ────────────────────────────────
# Требует отдельной установки через install.bat -> пункт [Intel GPU]:
# openvino>=2024.0
# optimum[openvino]
# transformers

# ── GigaAM v3 ONNX (русский ASR) ─────────────────────────────
# pip install onnx-asr[cpu,hub]

# ── Whisper HF (через HuggingFace transformers) ───────────────
# pip install transformers torch

# ── GGML (whisper.cpp через pywhispercpp) ────────────────────
# pip install pywhispercpp
